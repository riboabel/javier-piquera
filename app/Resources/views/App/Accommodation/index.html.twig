{% extends "frontend-requirejs.html.twig" %}

{% block page_content %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Reservas de hospedaje <small>listado</small></h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->

    <div class="row" style="margin-bottom: 10px;">
        <div class="col-lg-12">
            <div class="pull-right">
                <a href="#" class="btn btn-success">
                    <i class="fa fa-plus-circle"></i>
                    Crear reserva
                </a>
                <div class="btn-group">
                    <button class="btn btn-default" type="button" data-toggle="collapse" data-target="#filter-form" aria-expanded="false" aria-controls="filter-form"><span class="fa fa-filter"></span></button>
                    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                        Herramientas
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu pull-right" role="menu">
                        <li>
                            <a href="{{ path('app_accommodation_import') }}" role="button">
                                Importar
                            </a>
                        </li>
                        {#<li>#}
                            {#<a href="#" role="button" id="link-select-all">#}
                                {#<i class="glyphicon glyphicon-check"></i>#}
                                {#Seleccionar todos#}
                            {#</a>#}
                        {#</li>#}
                        {#<li>#}
                            {#<a href="#" role="button" id="link-select-none">#}
                                {#<i class="glyphicon glyphicon-unchecked"></i>#}
                                {#Borrar selección#}
                            {#</a>#}
                        {#</li>#}
                        {#<li class="divider"></li>#}
                        {#<li>#}
                            {#<a href="{{ path("app_reports_print_selection") }}" id="linkPrintSelection">#}
                                {#<i class="glyphicon glyphicon-print"></i>#}
                                {#Imprimir#}
                            {#</a>#}
                        {#</li>#}
                        {#<li>#}
                            {#<a href="{{ path('app_reports_print_special_selection') }}" id="linkPrintSpecialReport">#}
                                {#<i class="glyphicon glyphicon-print"></i>#}
                                {#Generar informe de excursiones#}
                            {#</a>#}
                        {#</li>#}
                    </ul>
                </div>
            </div>
        </div>
    </div>
    {#<div class="panel panel-default collapse" id="filter-form">#}
        {#<div class="panel-heading">#}
            {#<h4 class="panel-title">Opciones de filtro</h4>#}
        {#</div>#}
        {#<div class="panel-body">#}
            {#<form action="" role="form" id="filter">#}
                {#{{ form_row(form) }}#}
            {#</form>#}
        {#</div>#}
    {#</div>#}
    <div class="dataTable_wrapper">
        <table class="table table-striped table-bordered table-hover" id="table-accommodations" width="100%"></table>
    </div>
{% endblock %}

{% block page_javascripts %}
    <script>
        requirejs([
            'js/accommodations.index'
        ], function(app) {
            'use strict';

            app();
        });
    </script>
{% endblock %}