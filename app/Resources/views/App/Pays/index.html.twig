{% extends "frontend.html.twig" %}

{% block page_content %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Pagos pendientes <small>listado</small></h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->

    <div class="row" style="margin-bottom: 10px;">
        <div class="col-lg-12">
            <div class="pull-right">
                <div class="btn-group">
                    <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">Herramientas <span class="caret"></span></button>
                    <ul class="dropdown-menu pull-right" role="menu">
                        <li><a href="{{ path("app_pays_prepare") }}" id="linkPay">Pagar</a></li>
                        <li class="divider"></li>
                        <li><a href="#" id="linkSelectAll">Seleccionar todos</a></li>
                        <li><a href="#" id="linkSelectNone">Borrar selección</a></li>
                        <li class="divider"></li>
                        <li><a href="#" data-toggle="modal" data-target="#modal-historico">Ver pagos anteriores</a></li>
                    </ul>
                    <button type="button" class="btn btn-default" id="btnFilter"><i class="fa fa-search"></i></button>
                </div>
            </div>
        </div>
    </div>
    <div class="row" id="filter" style="display: none;">
        <div class="col-xs-12">
            <div class="panel panel-default">
                <div class="panel-heading">Opciones de filtro</div>
                <div class="panel-body">
                    <form action="" class="form-inline">
                        <div class="row">
                            <div class="col-xs-6 col-sm-3">
                                <label for="">Desde</label>
                                <input type="text" name="filter[startAt][from]" id="fromDate" class="datepicker form-control input-sm" value="">
                            </div>
                            <div class="col-xs-6 col-sm-3">
                                <label for="">Hasta</label>
                                <input type="text" name="filter[startAt][to]" id="toDate" class="datepicker form-control input-sm" value="">
                            </div>
                            <div class="col-xs-6 col-sm-3">
                                <label for="filter_driver">Conductor</label>
                                <select name="filter[driver]" id="filter_driver" class="form-control input-sm">
                                    <option value=""></option>
                                    {% for driver in drivers.result %}
                                        <option value="{{ driver.id }}">{{ driver }}</option>
                                    {% endfor %}
                                </select>
                            </div>
                            <div class="col-xs-6 col-sm-3">
                                <label for="filter_state">Estado</label>
                                <select name="filter[state]" id="filter_state" class="form-control input-sm">
                                    <option value=""></option>
                                    <option value="no-pagado" selected>No pagado</option>
                                    <option value="pagado">Pagado</option>
                                </select>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="dataTable_wrapper">
                <table class="table table-striped table-bordered table-hover" id="dataTables-pays" data-ajax-url="{{ path("app_pays_getdata") }}">
                    <thead>
                        <tr>
                            <th width="10" class="no-sort">&nbsp;</th>
                            <th>Fecha</th>
                            <th>Conductor</th>
                            <th>Agencia</th>
                            <th>Referencia</th>
                            <th>Tipo de servicio</th>
                            <th>Importe</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td colspan="6">&nbsp;</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <!-- /.table-responsive -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->

    {% if app.session.flashbag.has('notice') %}
        <div class="modal fade" data-for="flash-notice">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button class="close" data-dismiss="modal" type="button">&times;</button>
                        <h4 class="modal-title"></h4>
                    </div>
                    <div class="modal-body">
                        <div class="alert alert-success">
                            <ul>
                                {% for notice in app.session.flashbag.get('notice') %}
                                    <li>{{ notice }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-default btn-md" type="button" data-dismiss="modal"><i class="glyphicon glyphicon-ok"></i> Continuar</button>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}

    <div class="modal fade" data-for="pay-op" data-backdrop="static">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <button class="close" type="button" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Realizar pagos</h4>
                </div>
                <div class="modal-body">Cargando información de servicios seleccionados...</div>
                <div class="modal-footer">
                    <button class="btn btn-success btn-sm" type="button">
                        <i class="glyphicon glyphicon-ok"></i>
                        Continuar
                    </button>
                    <button class="btn btn-default btn-sm btn-print" data-url="{{ path("app_reports_print_pay_form") }}">
                        <i class="fa fa-print"></i>
                        Generar modelo de pago
                    </button>
                    <button class="btn btn-default btn-sm" data-dismiss="modal" type="button">
                        <i class="fa fa-undo"></i>
                        Cancelar operación
                    </button>
                </div>
            </div>
        </div>
    </div>
    <!-- /.modal.fade -->

    <div class="modal fade" data-for="pay-error">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button class="close" type="button" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Realizar pagos</h4>
                </div>
                <div class="modal-body">
                    <div class="alert alert-warning">
                        Debe seleccionar registros para esta operación
                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-default" type="button" data-dismiss="modal">
                        <i class="glyphicon glyphicon-ok"></i>
                        Continuar
                    </button>
                </div>
            </div>
        </div>
    </div>
    <!-- /.modal.fade -->

    <div class="modal fade" data-for="historico" id="modal-historico">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button class="close" type="button" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Histórico de pagos realizados</h4>
                </div>
                <div class="modal-body">
                    <form action="{{ path('app_pays_print') }}" method="post" role="form" target="_blank">
                        <p>
                            <select name="id" class="form-control">
                                {% for act in charges %}
                                    <option value="{{ act.id }}">{{ act.createdAt.format('d/m/Y H:i') }} - {{ act.charges.count }} servicios</option>
                                {% endfor %}
                            </select>
                        </p>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary">
                        <i class="glyphicon glyphicon-print"></i>
                        Imprimir
                    </button>
                    <button class="btn btn-default" type="button" data-dismiss="modal">
                        Cerrar
                    </button>
                </div>
            </div>
        </div>
    </div>
    <!-- /.modal.fade -->
{% endblock %}

{% block page_javascripts %}
    <script>
        $(document).ready(function() {
            App.Pagos.Index.init();
        });
    </script>
{% endblock %}
